pip install poetry  # ставим poetry как системный пакет (или через venv)

# Windows: добавить путь до poetry в PATH

poetry --version  # проверяем версию
poetry install  # устанавливаем виртуальное окружение (автоматически)
